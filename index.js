console.clear();

const container = require("./container");
require("./app").bootstrap(container);

// const { findByIdAndUpdate } = require("./test/models/User");

// findByIdAndUpdate(container.resolve("User"));